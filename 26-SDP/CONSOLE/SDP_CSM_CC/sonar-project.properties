# Required metadata
sonar.projectKey=CatalogueConsole
sonar.projectName=CatalogueConsole
sonar.projectVersion=1.0

# Comma-separated paths to directories with sources (required)
sonar.sources=src

# Comma-separated paths to directories with binaries (optional), in case of Java - directories with class files
sonar.binaries=build/classes

# Comma-separated paths to test source directories (optional)
#sonar.tests=test

# Comma-separated paths to files with third-party libraries (optional), in case of Java - JAR files
#sonar.libraries=lib/*.jar
sonar.libraries=C:/SDP/AVS/AVS_Workspace/_extlib_/tomcat/*.jar,C:/SDP/AVS/AVS_Workspace/_extlib_/primefaces/*.jar,C:/SDP/AVS/AVS_Workspace/_extlib_/*.jar,C:/SDP/AVS/AVS_Workspace/_extlib_/jaxws/*.jar,C:/SDP/AVS/AVS_Workspace/_extlib_/_clients/*.jar

# Language
sonar.language=java

# Encoding of the source files
sonar.sourceEncoding=UTF-8
 
